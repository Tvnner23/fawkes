# Bounded complete console snapshots

G15's authenticated deployment check reproduced HTTP 503 after guarded Git integration.
Its complete canonical response measured 300,321 UTF-8 bytes against a 256,000-byte
guard: 18 campaigns, 17 repository paths, complete roadmap and job recaps. This was not
a login, Pi connection, review or Git failure. No old record was deleted or reset.

The isolated correction raises only the full response ceiling to 512,000 bytes.
A complete synthetic projection using 32 copies of the heaviest retained campaign
measured 312,402 bytes through the unchanged aggregate activity window, leaving
about 64% headroom. A preliminary envelope estimated 807,723 bytes before accounting
for that window; it is not the actual serialized response. Neither measurement
creates canonical campaigns or establishes a universal maximum.
Larger responses still fail closed. Existing field/count limits, 32 KiB activity
window, 96,000-byte export limit, authentication and approval authority are unchanged.
Nothing is newly summarized, omitted or replaced to obtain a smaller response.

Qualification must include the full current canonical projection and export,
exact serialized-byte boundaries including Unicode, and an over-limit rejection.
Passing these checks is not independent acceptance or Pi deployment. Reuse the
accepted UI/timing/permission tests only for unchanged source. No additional live
approval or pinch trial is required by this backend-only change.

Keep G14 running until this exact successor is accepted and the complete browser
gate passes. Then use the normal canonical application/Git and metadata-only Pi
rollout with rollback. Physical swipe/Idle/Matrix verification remains pending.
